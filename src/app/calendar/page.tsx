import { PageTemplate } from "@/components/page-template";

export default function CalendarPage() {
  return (
    <PageTemplate
      title="Calendar"
      description="Manage your schedule, events, and training sessions"
    />
  );
} 